
extends ../application

block title
	| Delete member?

block document
	.document.paper
		.section
			h1 #{user.name}
		.section
			.pamphlet.tac
				form(method='post')
					p Are you sure you want to delete this member?
					input(type='hidden', name='action', value='delete')
					.btn-group
						a.btn.btn-primary(href='/members/'+user._id) Never Mind
						input.btn.btn-danger(type='submit', value='Yes, Destroy Them.')
